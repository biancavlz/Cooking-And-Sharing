<%= render "shared/page_title", title: @recipe.name %>

<div class="card">
  <div class="card-body">
    <p class="card-text"><%= @recipe.description %></p>
    <p class="card-text"><small class="text-muted">Created by <%= @recipe.user.name %> <%= time_ago_in_words(@recipe.created_at) %> ago</small></p>
  </div>
</div>