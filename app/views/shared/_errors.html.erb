<% if obj.errors.any? %>
  <div class="alert alert-danger" role="alert">
    <h4 class="alert-heading">
      <%= pluralize(obj.errors.count, "error") %>
      prohidited this <%= obj.class %> from being saved:
    </h4>
    
    <ul>
      <% obj.errors.full_messages.each do |error_msg| %>
        <li><%= error_msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
